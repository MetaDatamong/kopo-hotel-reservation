<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>호텔 찾아오는 길</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #0a0a0a;
            color: #d4af37;
        }

        .container {
            max-width: 1200px;
            margin: 100px auto 0;
            padding: 2rem;
            text-align: center;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 2rem;
        }

        .map {
            margin: 2rem auto;
            border-radius: 10px;
            overflow: hidden;
            max-width: 100%;
            height: 400px;
        }

        .address {
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 1rem;
            margin-top: 2rem;
            font-size: 1rem;
            line-height: 1.5;
        }

        .address h2 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }
    </style>
</head>

<body>
{{>layouts/header}}
    <div class="container">
        <h1>호텔 찾아오는 길</h1>

        <div id="map" class="map"></div>

        <div class="address">
            <h2>호텔 주소</h2>
            <p>럭셔리 호텔</p>
            <p>서울특별시 강남구 테헤란로 123</p>
            <p>서울, 06232</p>
            <p>전화: 02-1234-5678</p>
        </div>
    </div>

    <script>
        function initMap() {
            // 호텔의 고정된 위도와 경도 값
            var hotelLocation = { lat: 37.3860521, lng: 127.1214038 };
            
            // 구글 맵 API를 사용하지 않기 때문에 기본적으로 iframe을 삽입하는 방식으로 지도 삽입
            var mapDiv = document.getElementById('map');
            var iframe = document.createElement('iframe');
            iframe.width = "100%";
            iframe.height = "100%";
            iframe.style.border = "0";
            iframe.loading = "lazy";
            iframe.src = `https://www.google.com/maps/embed/v1/place?key=AIzaSyYOUR-KEY-HERE&q=${hotelLocation.lat},${hotelLocation.lng}&zoom=15`;

            // API 키 없이 사용하기 위한 URL
            iframe.src = `https://maps.google.com/maps?q=${hotelLocation.lat},${hotelLocation.lng}&z=15&output=embed`;
            mapDiv.appendChild(iframe);
        }

        // 지도 로드
        window.onload = initMap;
    </script>

</body>

</html>
