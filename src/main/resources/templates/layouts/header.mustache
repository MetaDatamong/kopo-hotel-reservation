<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>헤더</title>
    <style>
        nav {
            background-color: rgba(10, 10, 10, 0.8);
            padding: 1rem;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        nav ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
            display: flex;
            justify-content: center;
        }
        nav ul li {
            margin: 0 1rem;
            position: relative;
        }
        nav ul li a {
            text-decoration: none;
            color: #d4af37;
            font-weight: bold;
            transition: color 0.3s ease;
        }
        nav ul li a:hover {
            color: #ffffff;
        }
        .dropdown-content {
            display: none;
            position: absolute;
            background-color: rgba(10, 10, 10, 0.9);
            min-width: 160px;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.5);
            z-index: 1;
        }
        nav ul li:hover .dropdown-content {
            display: block;
        }
        .dropdown-content a {
            color: #d4af37;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
            transition: background-color 0.3s ease;
        }
        .dropdown-content a:hover {
            background-color: #d4af37;
            color: #0a0a0a;
        }

        /* Login and Signup Button Styles */
        .auth-buttons {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-right: 2rem;
        }
        .auth-buttons a {
            text-decoration: none;
            color: #d4af37;
            font-weight: bold;
            transition: color 0.3s ease;
        }
        .auth-buttons a:hover {
            color: #ffffff;
        }

        /* Username Greeting */
        .username-greeting {
            color: #d4af37;
            font-weight: bold;
            margin-right: 1rem;
        }

        /* Logo Style */
        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: #d4af37;
            text-decoration: none;
        }
        .logo:hover {
            color: #ffffff;
        }

        /* Message styles */
        .alert {
            color: #ffffff; /* Font color for the alert message */
            background-color: #dc3545; /* Background color for the alert */
            border-color: #dc3545;
            padding: 15px;
            margin: 100px auto 20px; /* Center the alert and add top margin */
            border-radius: 5px;
            max-width: 600px; /* Maximum width of the alert */
            height: 50px; /* Set a fixed height for the alert */
            display: flex; /* Use flexbox for centering content */
            align-items: center; /* Vertically center the content */
            justify-content: center; /* Horizontally center the content */
            position: relative;
            text-align: center; /* Center the text */
        }
        .alert svg {
            vertical-align: middle;
            margin-right: 10px;
        }
        
    </style>
</head>
<body>

<nav>
    <a href="/mkhotel/index" class="logo">MK</a>
    <ul>
        <li>
            <a href="#">리조트 소개</a>
            <div class="dropdown-content">
                <a href="/mkhotel/roomInfo">객실 안내</a>
                <a href="/mkhotel/wayToMk">오시는 길</a>
            </div>
        </li>
        <li>
            <a href="#">예약</a>
            <div class="dropdown-content">
                <a href="/mkhotel/bookRoom">예약하기</a>
                <a href="/mkhotel/bookDetail">예약확인</a>
            </div>
        </li>
        <li>
            <a href="#">고객센터</a>
            <div class="dropdown-content">
                <a href="/mkhotel/notice">공지사항</a>
            </div>
        </li>
        <li><a href="/mkhotel/admin/management">관리 페이지</a></li>
    </ul>
    <div class="auth-buttons">
        {{#isLoggedIn}}
        <span class="username-greeting">안녕하세요 {{isLoggedIn}} 님</span>
        <a href="/mkhotel/logout">로그아웃</a>
        {{/isLoggedIn}}
        {{^isLoggedIn}}
        <a href="/mkhotel/login">로그인</a>
        <a href="/mkhotel/join">회원가입</a>
        {{/isLoggedIn}}
    </div>
</nav>

{{#message}}
<div class="alert alert-danger alert-dismissible fade show" role="alert">
  <svg class="bi flex-shrink-0 me-2" width="24" height="24" role="img" aria-label="Danger:">
    <use xlink:href="#exclamation-triangle-fill"/>
  </svg>
  <div>
    {{message}}
  </div>
</div>
{{/message}}

</body>
</html>
